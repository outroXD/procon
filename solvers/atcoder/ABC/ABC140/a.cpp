#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep2(i, s, n) for (int i = s; i < (int)(n); i++)

#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int N; cin >> N;

    cout << pow(N, 3) << endl;
}