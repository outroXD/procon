#define rep(i, a, b) for(int i = a; i < b; i++)
#define rrep(i, a, b) for(int i = a; i <= b; i++)

#include <iostream>

using namespace std;

int main()
{
    int a, b, h; cin >> a >> b >> h;

    cout << ((a+b)*h)*0.5 << endl;
}